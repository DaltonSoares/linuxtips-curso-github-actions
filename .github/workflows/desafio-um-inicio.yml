name: Exercitando desafio-um

on:
  push:
    branches:
      - desafio-um

jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - name: baixando imagem docker
        run: docker pull nginx:latest

      - name: listando imagens disponiveis
        run: docker image ls